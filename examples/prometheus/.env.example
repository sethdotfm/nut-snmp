# Credentials upsd will accept. nut_exporter authenticates with these, so
# NUT_EXPORTER_PASSWORD in docker-compose.yml must match NUT_PASSWORD here.
NUT_USER=nutmon
NUT_PASSWORD=change-me

LISTEN_ADDR=0.0.0.0
LISTEN_PORT=3493
